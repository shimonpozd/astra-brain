# Brain service environment template.
#
# Copy this file to .env (never commit the real file) and populate the values
# with deployment-specific secrets.  The Docker compose example uses the same
# format via an env_file reference.

# ----- Core service ---------------------------------------------------------
# Public port the FastAPI service listens on.
BRAIN_PORT=7030

# Shared Redis instance for STM, config broadcasts, etc.
REDIS_URL=redis://redis:6379/0

# PostgreSQL connection string (asyncpg driver)

DATABASE_URL=postgresql+asyncpg://astra:astra@localhost:5432/astra_brain

# Authentication token for the admin endpoints.
ADMIN_TOKEN=change-me

# JWT configuration
JWT_SECRET=super-secret-jwt
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=60

# When running inside Docker, the config package is expected at /app/config.
# If you mount it elsewhere, point ASTRA_CONFIG_ROOT to the parent directory
# that contains the `config` package.
# ASTRA_CONFIG_ROOT=/app

# Uvicorn workers (set >1 only when the service runs behind a process manager).
# UVICORN_WORKERS=1

# ----- External APIs --------------------------------------------------------
# Uncomment the providers you actually use and fill the secrets at deploy time.
# OPENAI_API_KEY=
# OPENAI_ORG_ID=
# OPENROUTER_API_KEY=
# OPENROUTER_REFERRER=https://astra.chat
# ELEVENLABS_API_KEY=
# DEEPGRAM_API_KEY=

# ---------------------------------------------------------------------------
# Keep production secrets in a secure secret manager where possible.  Only use
# .env files for local development or when you can guarantee safe storage.
